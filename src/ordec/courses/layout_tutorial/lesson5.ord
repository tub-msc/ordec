from ordec.core import *
from ordec.lib.ihp130 import SG13G2
from ordec.layout import SRouter

cell Example:
    viewgen layout(self) -> Layout:
        .ref_layers = SG13G2().layers
        layers = .ref_layers

        LayoutRect pad_w: .layer=layers.Metal1; .rect=(0, 0, 600, 600)
        LayoutRect pad_e: .layer=layers.Metal1; .rect=(3400, 0, 4000, 600)

        # A Metal1 bar between the pads. The connection from pad_w to
        # pad_e must not touch it.
        LayoutRect obstacle: .layer=layers.Metal1; .rect=(1800, -1000, 2100, 1600)

        # EDIT HERE (route)
