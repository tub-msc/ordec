from ordec.core import *
from ordec.lib.ihp130 import SG13G2

cell Example:
    viewgen layout(self) -> Layout:
        .ref_layers = SG13G2().layers
        layers = .ref_layers

        LayoutRect wall_a: .layer=layers.Metal1; .rect=(0, 0, 800, 2800)
        LayoutRect wall_b: .layer=layers.Metal1; .rect=(1200, 800, 1800, 1600)

        # EDIT HERE (clearance)

        LayoutRect dev_a: .layer=layers.Activ; .rect=(4000, 0, 4800, 800)
        LayoutRect dev_b: .layer=layers.Activ; .rect=(5600, 1600, 6400, 2400)

        # EDIT HERE (well)
