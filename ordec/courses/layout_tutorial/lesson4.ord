from ordec.core import *
from ordec.lib.ihp130 import SG13G2

cell Example:
    viewgen layout -> Layout:
        .ref_layers = SG13G2().layers
        layers = .ref_layers

        LayoutRect base: .layer=layers.Metal1; .rect=(0, 0, 6000, 800)

        # EDIT HERE (pads)
