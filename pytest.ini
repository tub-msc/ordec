[pytest]
minversion = 6.0
addopts =
    --ignore=ordec
    --tb=native
    --cov=ordec --cov-report html
testpaths =
    tests
markers =
    web
#tmp_path_retention_policy = failed
#tmp_path_retention_count = 3
